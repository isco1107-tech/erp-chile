import { Anton } from 'next/font/google';

/** Titulares condensados de la landing; el cuerpo sigue con la sans del sitio. */
export const displayFont = Anton({ weight: '400', subsets: ['latin'], variable: '--font-display', display: 'swap' });
